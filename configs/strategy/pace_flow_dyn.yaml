_target_: src.strategies.pace_flow_dyn.PACEStrategy

# Canonical length knob shared by both models
max_new_tokens: 128

# Common decoding params
steps: 128
temperature: 0.0
cfg_scale: 0.0

# LLaDA-related params (ignored by Dream)
block_length: 32
mask_id: 126336
eos_token_id: 126081
eot_token_id: 126348
logits_eos_inf: false
confidence_eos_eot_inf: false

# Dream-related params (ignored by LLaDA)
top_p: 0.95
top_k: null
alg_temp: 0.0
eps: 1e-3

# PACE-Flow-Dyn hyperparameters
num_particles: 4
q0: 0.15
tau_c_min: 0.70
tau_c_max: 0.90
lock: 2
r0: 0.01
ve_quantile_hi: 0.9
s_quantile_lo: 0.1
early_stop_ve_eps: null

# Particle logging
log_particles: true
log_particles_to_cpu: true
log_particles_filename: pace_particles.pt
