defaults:
  - model: llada
  - strategy: llada_native
  - eval: sudoku

batch_size: 1
seed: 0

hydra:
  run:
    dir: output/${slug:${hydra:runtime.choices.model}}_${slug:${hydra:runtime.choices.strategy}}_${slug:${hydra:runtime.choices.eval}}_steps${strategy.steps}_${now:%Y%m%d-%H%M%S}
